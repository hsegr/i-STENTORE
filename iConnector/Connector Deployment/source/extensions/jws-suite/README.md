# JWS Suite Extension

This extension registers the `Jws2020SignatureSuite`, in order to support JSON Web Tokens and JSON Web Keys in DIDs.

```
    implementation(project(":extensions:jws-suite"))
```

The extension should be included in the Connector.

