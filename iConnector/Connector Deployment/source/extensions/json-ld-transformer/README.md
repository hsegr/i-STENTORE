# JSON-LD transformer

This extension adds a transformer to properly support JSON-LD.

```
    implementation(project(":extensions:json-ld-transfromer"))
```

The extension should be included in the Connector.

