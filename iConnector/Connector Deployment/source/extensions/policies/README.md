# Policies extension

This extension adds the necessary policy handlers for the negotiations automation and the access control of the connector catalog.

In order to import this extension, add the following to the build file:

```
    implementation(project(":extensions:policies"))
```

The extension should be included in the Connector.
